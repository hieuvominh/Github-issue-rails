<%= form_with(model: gitissue, local: true) do |form| %>
  <% if gitissue.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(gitissue.errors.count, "error") %> prohibited this gitissue from being saved:</h2>

      <ul>
      <% gitissue.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content %>
  </div>

  <div class="field">
    <%= form.label :link %>
    <%= form.text_field :link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
